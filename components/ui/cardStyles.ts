/**
 * Shared surface/control class constants. These exist so a design refresh can
 * swap tokens in one place instead of editing every component.
 */

/** Elevated "floating" card used by Portfolio and Home feature surfaces. */
export const FLOATING_CARD =
  "rounded-3xl bg-surface-raised shadow-card ring-1 ring-hairline";

/** Secondary action button: bordered surface rectangle. */
export const SECONDARY_BUTTON =
  "rounded-xl border border-hairline bg-surface py-2.5 text-sm font-medium text-ink-secondary";
